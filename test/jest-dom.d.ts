/**
 * Ensina o TypeScript os matchers do jest-dom (`toBeInTheDocument`, `toHaveClass`).
 * O `jest.setup.js` já os carrega em tempo de execução; isto é só para o `tsc`.
 */
import "@testing-library/jest-dom";
